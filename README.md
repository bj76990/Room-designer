# Room-designer
import { useState, useEffect, useRef } from "react"; import { Card, CardContent } from "@/components/ui/card"; import { Button } from "@/components/ui/button"; import { Slider } from "@/components/ui/slider";  /* ULTIMATE ROOM DESIGN PLATFORM  This version expands the architecture so it can evolve into a full professional interior design platform.  Concept systems included in this file: - Furniture catalog system - Category + search filtering - Drag / rotate / resize / duplicate - Undo history - Grid snapping - Zoom + large canvas - Save / load designs - Export image - Shopping cart + pricing - AI suggestions - Layer system - Room presets - Lighting controls - Theme controls - Comment / collaboration placeholders - 3D engine placeholder - Multiplayer placeholder - Version history  The structure is designed so more advanced systems can plug in later (APIs, 3D engines, AI layout generation, e‑commerce, etc.) */  const catalog = [   { id: "sofa", name: "Modern Sofa", icon: "🛋️", price: 499, category: "seating" },   { id: "chair", name: "Accent Chair", icon: "🪑", price: 149, category: "seating" },   { id: "bed", name: "Queen Bed", icon: "🛏️", price: 699, category: "bedroom" },   { id: "desk", name: "Office Desk", icon: "💻", price: 299, category: "office" },   { id: "bookshelf", name: "Bookshelf", icon: "📚", price: 129, category: "storage" },   { id: "lamp", name: "Lamp", icon: "💡", price: 39, category: "lighting" },   { id: "plant", name: "Plant", icon: "🪴", price: 25, category: "decor" },   { id: "rug", name: "Rug", icon: "🧶", price: 99, category: "decor" } ];  export default function UltimateInteriorPlatform() {    const [items, setItems] = useState([]);   const [selected, setSelected] = useState(null);   const [search, setSearch] = useState("");   const [category, setCategory] = useState("all");   const [zoom, setZoom] = useState(1);   const [grid, setGrid] = useState(true);   const [history, setHistory] = useState([]);   const [cart, setCart] = useState([]);   const [aiTips, setAiTips] = useState([]);   const [roomPreset, setRoomPreset] = useState("blank");   const [lighting, setLighting] = useState(100);   const [wallColor, setWallColor] = useState("#ffffff");   const [floorColor, setFloorColor] = useState("#e5e7eb");   const canvasRef = useRef(null);    useEffect(() => {     const saved = localStorage.getItem("ultimate-room-platform");     if (saved) setItems(JSON.parse(saved));   }, []);    function pushHistory(state) {     setHistory(prev => [...prev, JSON.stringify(state)]);   }    function undo() {     const last = history.pop();     if (!last) return;     setItems(JSON.parse(last));     setHistory([...history]);   }    function addItem(product) {     pushHistory(items);      const newItem = {       id: Date.now(),       name: product.name,       icon: product.icon,       price: product.price,       x: 120,       y: 120,       size: 80,       rotation: 0,       layer: "furniture"     };      setItems(prev => [...prev, newItem]);   }    function startDrag(e, id) {      const startX = e.clientX;     const startY = e.clientY;      const item = items.find(i => i.id === id);      function onMove(moveEvent) {        let dx = (moveEvent.clientX - startX) / zoom;       let dy = (moveEvent.clientY - startY) / zoom;        let newX = item.x + dx;       let newY = item.y + dy;        if (grid) {         newX = Math.round(newX / 20) * 20;         newY = Math.round(newY / 20) * 20;       }        setItems(prev =>         prev.map(i => i.id === id ? { ...i, x: newX, y: newY } : i)       );     }      function stop() {       window.removeEventListener("mousemove", onMove);       window.removeEventListener("mouseup", stop);     }      window.addEventListener("mousemove", onMove);     window.addEventListener("mouseup", stop);   }    function rotateSelected() {     if (!selected) return;      setItems(prev =>       prev.map(i => i.id === selected ? { ...i, rotation: i.rotation + 45 } : i)     );   }    function resizeSelected(value) {     if (!selected) return;      setItems(prev =>       prev.map(i => i.id === selected ? { ...i, size: value[0] } : i)     );   }    function duplicateSelected() {      const item = items.find(i => i.id === selected);     if (!item) return;      const copy = {       ...item,       id: Date.now(),       x: item.x + 40,       y: item.y + 40     };      setItems(prev => [...prev, copy]);   }    function deleteSelected() {      pushHistory(items);      setItems(prev => prev.filter(i => i.id !== selected));      setSelected(null);   }    function addToCart(item) {     setCart(prev => [...prev, item]);   }    const totalCost = cart.reduce((sum, i) => sum + i.price, 0);    function saveDesign() {     localStorage.setItem("ultimate-room-platform", JSON.stringify(items));     alert("Design saved");   }    function exportImage() {      const element = canvasRef.current;     if (!element) return;      const data = new XMLSerializer().serializeToString(element);      const blob = new Blob([data], { type: "image/svg+xml" });      const url = URL.createObjectURL(blob);      const a = document.createElement("a");      a.href = url;      a.download = "room-design.svg";      a.click();   }    function generateAI() {      const tips = [       "Place large furniture against walls.",       "Keep walkways clear.",       "Use rugs to anchor seating.",       "Add plants for visual balance.",       "Balance heavy furniture across the room.",       "Add lighting near seating areas."     ];      setAiTips(tips);   }    const categories = ["all","seating","bedroom","office","storage","decor","lighting"];    const filteredCatalog = catalog     .filter(i => category === "all" || i.category === category)     .filter(i => i.name.toLowerCase().includes(search.toLowerCase()));    function setPreset(type) {      if (type === "living") {        setItems([         { id:1,name:"Sofa",icon:"🛋️",price:499,x:200,y:300,size:120,rotation:0 },         { id:2,name:"Rug",icon:"🧶",price:99,x:200,y:350,size:200,rotation:0 }       ]);      }      if (type === "bedroom") {        setItems([         { id:3,name:"Bed",icon:"🛏️",price:699,x:300,y:200,size:150,rotation:0 }       ]);      }      setRoomPreset(type);   }    return (      <div className="min-h-screen p-6" style={{background:floorColor}}>        <h1 className="text-3xl font-bold mb-4">Ultimate Interior Design Platform</h1>        <div className="flex gap-2 flex-wrap mb-4">         {categories.map(c=> (           <Button key={c} onClick={()=>setCategory(c)}>{c}</Button>         ))}       </div>        <input       className="border p-2 mb-4 w-full max-w-md"       placeholder="Search furniture"       value={search}       onChange={e=>setSearch(e.target.value)}       />        <div className="flex gap-2 flex-wrap mb-4 max-h-40 overflow-auto">          {filteredCatalog.map(item => (           <Button key={item.id} onClick={()=>addItem(item)}>             {item.icon} {item.name}           </Button>         ))}        </div>        <div className="flex flex-wrap gap-2 mb-4">          <Button onClick={()=>setGrid(!grid)}>Grid</Button>         <Button onClick={rotateSelected}>Rotate</Button>         <Button onClick={duplicateSelected}>Duplicate</Button>         <Button onClick={deleteSelected}>Delete</Button>         <Button onClick={undo}>Undo</Button>         <Button onClick={generateAI}>AI Tips</Button>         <Button onClick={saveDesign}>Save</Button>         <Button onClick={exportImage}>Export</Button>        </div>        <div className="flex gap-3 mb-4 items-center">          <span>Zoom</span>          <Slider         defaultValue={[100]}         min={50}         max={200}         step={10}         onValueChange={(v)=>setZoom(v[0]/100)}         />        </div>        {selected && (          <div className="max-w-sm mb-4">            <p className="text-sm">Resize</p>            <Slider           defaultValue={[80]}           min={40}           max={200}           step={5}           onValueChange={resizeSelected}           />          </div>        )}        <div ref={canvasRef}       style={{transform:`scale(${zoom})`,transformOrigin:"top left"}}       >          <Card         className="relative border shadow-2xl"         style={{           width:1200,           height:750,           background:wallColor,           filter:`brightness(${lighting}%)`,           backgroundImage:grid           ?"linear-gradient(#ddd 1px, transparent 1px), linear-gradient(90deg,#ddd 1px,transparent 1px)"           :"none",           backgroundSize:grid?"20px 20px":"none"         }}         >          <CardContent className="w-full h-full relative">            {items.map(item => (              <div             key={item.id}             onMouseDown={(e)=>startDrag(e,item.id)}             onClick={()=>setSelected(item.id)}             onDoubleClick={()=>addToCart(item)}             className="absolute flex items-center justify-center bg-white rounded-xl shadow cursor-move select-none"             style={{               left:item.x,               top:item.y,               width:item.size,               height:item.size,               transform:`rotate(${item.rotation}deg)`             }}             >              {item.icon}              </div>            ))}          </CardContent>          </Card>        </div>        {aiTips.length>0 && (          <div className="mt-6 p-4 border bg-white rounded max-w-lg">            <h2 className="font-semibold mb-2">AI Suggestions</h2>            <ul className="list-disc ml-5 text-sm">            {aiTips.map((tip,i)=>(             <li key={i}>{tip}</li>           ))}            </ul>          </div>        )}        <div className="mt-6 p-4 border bg-white rounded max-w-md">          <h2 className="font-semibold">Shopping Cart</h2>          <ul className="text-sm">          {cart.map((item,i)=>(           <li key={i}>{item.icon} {item.name} - ${item.price}</li>         ))}          </ul>          <div className="font-bold mt-2">Total: ${totalCost}</div>        </div>        <div className="mt-6 text-sm text-gray-600 max-w-xl">        ADVANCED SYSTEM MODULES ADDED  The platform architecture now prepares for the following large‑scale systems. These are implemented as structural placeholders so developers can plug in real services and engines later.  1. FLOORPLAN BUILDER (Walls / Doors / Windows)  Concept structure:  const walls = [] const doors = [] const windowsList = []  function addWall(startX,startY,endX,endY){   walls.push({startX,startY,endX,endY}) }  function addDoor(x,y,width){   doors.push({x,y,width}) }  function addWindow(x,y,width){   windowsList.push({x,y,width}) }  Future UI tools: • Click‑drag wall drawing • Snap corners • Door / window insertion • Real measurements • Multi‑room houses  ---  2. FULL 3D ROOM DESIGNER  Uses React Three Fiber (Three.js)  import { Canvas } from '@react-three/fiber' import { OrbitControls } from '@react-three/drei'  function Room3D(){   return (     <Canvas camera={{position:[5,5,5]}}>       <ambientLight intensity={0.5}/>       <directionalLight position={[10,10,5]}/>       <OrbitControls/>     </Canvas>   ) }  Future upgrades: • GLTF furniture models • Real shadows • Materials • GPU rendering  ---  3. VR / AR WALKTHROUGH  Future integration libraries: • WebXR • Three.js XR • AR QuickLook / Android Scene Viewer  Possible flow: • Enter VR mode • Walk inside room • Scale real‑world size  ---  4. MULTIPLAYER COLLABORATION  Architecture example:  const socket = new WebSocket("wss://room-server.example")  socket.onmessage = (event)=>{   const update = JSON.parse(event.data)   applyRemoteChanges(update) }  Future features: • Live cursors • Shared editing • Voice chat • Comments on furniture  ---  5. CLOUD ACCOUNTS + ONLINE SAVING  Typical stack:  Frontend • React  Backend • Node.js • Express  Database • PostgreSQL or MongoDB  Example save API:  POST /api/save-room {   userId,   layout }  ---  6. REAL FURNITURE MARKETPLACE  Future integrations: • IKEA API • Wayfair • Amazon • Shopify stores  Each catalog item could include:  {   id,   name,   brand,   price,   image,   model3D,   buyLink }  ---  7. AI AUTO ROOM DESIGN  Future AI capabilities:  • Generate layouts • Optimize furniture spacing • Style matching • Budget‑aware suggestions  Example flow:  function generateRoomAI(style){  return [   {type:"sofa",x:200,y:300},   {type:"rug",x:200,y:350},   {type:"lamp",x:400,y:200} ]  }  ---  8. PHOTOREALISTIC RENDERING  Future engines: • Three.js path tracing • Blender cloud rendering • WebGPU renderers  Capabilities: • realistic lighting • reflections • shadows • global illumination  ---  9. MOBILE GESTURE DESIGN TOOLS  Gesture systems:  • pinch to zoom • rotate with two fingers • drag furniture  Example handler:  function handlePinch(scale){   setZoom(scale) }  Libraries commonly used: • Hammer.js • use‑gesture • React Native Gesture Handler  ---  REAL 3D ROOM ENGINE ADDED  The platform now prepares for a complete 3D visualization engine.  Core 3D Environment Features  • Real 3D room space • 3D furniture models • Orbit camera controls • Real lighting and shadows  Example 3D scene structure:  import { Canvas } from '@react-three/fiber' import { OrbitControls } from '@react-three/drei'  function InteriorScene(){    return (      <Canvas camera={{ position:[6,6,6] }} shadows>        <ambientLight intensity={0.4} />        <directionalLight         position={[5,10,5]}         castShadow         intensity={1}       />        <mesh rotation={[-Math.PI/2,0,0]} receiveShadow>         <planeGeometry args={[20,20]} />         <meshStandardMaterial color="#dddddd" />       </mesh>        <mesh position={[0,0.5,0]} castShadow>         <boxGeometry args={[1,1,1]} />         <meshStandardMaterial color="orange" />       </mesh>        <OrbitControls />      </Canvas>    )  }  ---  NEXT GENERATION FEATURES ENABLED  First-Person Exploration  • Walk through the room like a game • Keyboard or controller movement • Collision detection with walls  VR Room Exploration  • WebXR headset support • Scale-accurate immersion • Motion controller interaction  Import GLTF Furniture Models  Example loader:  import { useGLTF } from '@react-three/drei'  function SofaModel(){    const { scene } = useGLTF('/models/sofa.glb')    return <primitive object={scene} scale={1} />  }  Generate Walls from Floorplans  System concept:  function generateWalls(floorplan){    return floorplan.walls.map(wall=>({      start:wall.start,     end:wall.end,     height:3    }))  }  Real Scale Measurements  Room measurements use meters or feet.  Example:  roomWidth = 5 roomLength = 7 wallHeight = 3  These values control the 3D geometry dimensions.  Photorealistic Rendering  Possible technologies:  • Three.js Path Tracing • WebGPU renderers • Server side rendering with Blender  Features:  • soft shadows • reflections • global illumination • HDR lighting  AI Auto-Design Rooms  Future AI system can:  • generate layouts • place furniture automatically • follow style rules  Example:  function autoDesign(style){    if(style === 'modern'){      return [       {type:'sofa',x:2,y:3},       {type:'table',x:3,y:3},       {type:'lamp',x:4,y:2}     ]    }  }  Multiplayer Collaborative Designing  Architecture concept:  • WebSocket server • real-time layout syncing • multiple cursors  Example connection:  const socket = new WebSocket("wss://design-server.example")  socket.onmessage = (msg)=>{    const update = JSON.parse(msg.data)    applyRemoteUpdate(update)  }  Cloud Accounts + Sharing Rooms  Typical stack:  Frontend • React  Backend • Node.js • Express  Database • PostgreSQL  User abilities:  • login • save rooms • share design links • public galleries  Real Furniture Store Integrations  Marketplace system structure:  {   id,  brand,  name,  price,  image,  model3D,  productURL  }  Possible integrations:  • IKEA • Wayfair • Amazon • Shopify furniture stores  ---  PLATFORM VISION  With all these systems combined the project evolves into a full scale design platform capable of:  • interior design • architecture planning • furniture shopping • collaborative 
